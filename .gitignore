# Python
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
.venv-build/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# Downloads (user-local content)
downloads/

# 登录态 profile(含抖音账号 cookie，绝不可提交)
douyin_profile/

# 构建产物/日志(日志通配 *.log 兜底，已含 build_run.log/build_venv.log)
build.log
# 只忽略根目录旧的图标预览 png；assets/ 下的图标资源(含 .png)需要提交，已放行
/app_icon.png
*.log
gui_stdio.log
login_error.log
# 打包期 runtime hook 填充产物(源码模板 _runtime_hook.py 才是正式文件)
_runtime_hook_filled.py
# 临时调试 runner/诊断脚本(不入库)
_run_debug.py
_diag.py

# 测试缓存
.pytest_cache/
.coverage

# OS
.DS_Store
Thumbs.db
desktop.ini
